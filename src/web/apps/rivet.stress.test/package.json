{
    "name": "rivet.stress.test",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "dev:shell": "rivet dev --shell",
        "build": "vue-tsc --noEmit -p tsconfig.json && vue-tsc --noEmit -p tsconfig.node.json && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "@microsoft/signalr": "^10.0.0",
        "@microsoft/signalr-protocol-msgpack": "^10.0.0",
        "@rivet/client": "workspace:*",
        "vue": "^3.5.0"
    },
    "devDependencies": {
        "@rivet/cli": "workspace:*",
        "@rivet/shell": "workspace:*",
        "@types/node": "^25.6.0",
        "@vitejs/plugin-vue": "^6.0.6",
        "typescript": "^5.8.0",
        "vite": "^8.0.0",
        "vue-tsc": "^2.0.0"
    }
}
