{
  "name": "rivet-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter mysoow.toolkit dev",
    "dev:shell": "pnpm --filter mysoow.toolkit dev:shell",
    "build": "pnpm --filter mysoow.toolkit build",
    "dev:stress": "pnpm --filter rivet.stress.test dev",
    "dev:stress:shell": "pnpm --filter rivet.stress.test dev:shell",
    "build:stress": "pnpm --filter rivet.stress.test build"
  }
}
