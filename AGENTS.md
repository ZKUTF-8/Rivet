# Rivet 仓库通用规则

本文件是整个仓库的 Agent 规则入口，负责提供完整的项目背景和全局约束。子目录中的 `AGENTS.md` 只补充对应技术栈或项目的局部规则；处理文件前，应读取从仓库根目录到目标文件所在目录沿途的全部 `AGENTS.md`。已有规则已经说明的项目背景，不应要求用户在后续对话中重复提供；后续对话主要补充本次功能、问题和验收目标。

越接近目标文件的规则越具体；若规则发生冲突，优先采用更具体目录中的规则。规则不是不可修订的历史记录：当规则、文档、配置和真实代码不一致时，先判断是实现缺陷、正在进行的架构迁移，还是规则已经过期。不要为了迎合过期规则改坏业务代码，也不要无声忽略仍然有效的约束。确认属于长期约定变化时，应在同一次修改中更新对应 `AGENTS.md`，让规则随项目演进。

## 沟通与文档

- 对话、提交说明、代码注释和中文主文档统一使用简体中文。
- 代码标识符、包名、类型名、协议名和行业通用技术关键词可以保留英文。
- `README_EN.md` 等明确的英文翻译文件可以使用英文；工具自动生成的 schema、清单和第三方内容不强制翻译。
- git commit 消息使用中文，描述修改目的，不只罗列文件名。

## 项目全景

Rivet 是一个 .NET + Vue 上位机快速开发框架：.NET 负责硬件和业务逻辑，Vue 负责界面，Tauri v2 提供可选桌面壳。后端通过 `[JsCallable]`、`[JsEvent]`、可选的 `[JsBindable]` 和公开 `Rv<T>` 属性声明契约；`Rivet.Generator` 生成 `rivet.contract.json`，`@rivet/cli` 再生成 TypeScript 代理，业务前端最终通过全局 `rv` 使用后端能力。

仓库当前同时包含三类项目，修改前必须先判断目标属于哪一类：

- **框架核心**：`Rivet.Core`、`Rivet.Generator`、`@rivet/client`、`@rivet/cli` 和 `rivet.shell`。这些项目形成可复用框架，强调稳定边界、最小公开 API 和跨项目兼容。
- **测试与验证项目**：`rivet.stress.test.server` 和 `rivet.stress.test`。它们用于协议、吞吐量、延迟、并发和渲染性能验证，不等同于正式的自动化单元测试，也不代表普通业务应用的默认写法。
- **落地应用**：`mysoow.toolkit.server` 和 `mysoow.toolkit`。Mysoow.Toolkit 是内部工具，也是 Rivet 第一个真实落地的实验应用，用于验证框架能否服务实际业务；它既不是一次性演示，也不应反向承载框架内部实现。

框架核心、测试项目和应用项目当前放在同一仓库，通过本地项目引用和 pnpm workspace 协作。新增项目时沿用 `core` 与 `apps` 的职责划分，不要把业务实现放进框架核心，也不要用测试项目的特殊做法定义框架默认行为。

## 仓库结构

```text
src/server/
  core/        .NET 框架核心和 Source Generator
  apps/        压测服务和业务应用服务
src/web/
  core/        浏览器运行时和开发 CLI
  apps/        压测前端和业务应用前端
src/shell/
  core/        Tauri 通用桌面壳
```

- 新代码继续放在 `src/server`、`src/web`、`src/shell`。
- 目录名使用小写加点号，例如 `rivet.core`、`rivet.stress.test.server`、`mysoow.toolkit`。
- 框架核心、测试项目和落地应用当前通过本地项目引用或 pnpm workspace 协作；发布流程成熟前，不要擅自改成远程 NuGet/npm 依赖。

## 强制文档注释

以下要求适用于所有人工编写的 C#、TypeScript、JavaScript、Vue 和 Rust 代码，不区分 `public`、`internal`、`protected` 或 `private`。工具生成文件、第三方代码和 schema 不适用，应修改生成源而不是补写注释。

- 所有类型、接口、枚举、委托、构造函数、函数、方法、属性、字段、事件和常量都必须有符合语言惯例的中文文档注释。
- C# 使用 XML 文档注释 `///`。每个参数必须写 `<param>`，泛型参数写 `<typeparam>`，存在返回值时写 `<returns>`；有重要且稳定的异常契约时写 `<exception>`。
- TypeScript、JavaScript 和 Vue `<script>` 使用 `/** ... */` JSDoc/TSDoc。所有函数，包括内部函数、箭头函数、组件方法和业务回调，都必须说明职责；每个参数都必须写 `@param`，存在返回值时写 `@returns`。
- TypeScript/JavaScript 的类字段、对象属性、接口成员、类型别名、枚举、常量、Vue props、emits、响应式状态、计算属性和模板引用都必须有中文 JSDoc/TSDoc 或紧邻的文档注释。
- Rust 类型、trait、函数、方法、字段、常量和模块使用 `///` 或 `//!` 文档注释；参数和返回语义在正文中说明。
- 注释重点说明职责、参数语义、设计原因、生命周期、线程或协议边界，不能只翻译标识符和代码表达式，也不能使用无内容的模板注释。

## 通用修改原则

- 先理解现有边界和调用链，再修改实现；不要只根据设计文档推断当前能力。
- 不要为了单次调用的一行逻辑额外封装方法；只有抽象能够隐藏复杂度、表达边界或形成复用时才拆分。
- 保留用户已有的无关改动，不进行无关格式化、批量重命名或目录整理。
- 不手工修改 `.rivet`、`bin`、`obj`、`dist`、Tauri `gen/schemas` 等生成物，应修改生成源或生成流程。
- 修改依赖版本时同步提交对应 lockfile；没有依赖变化时不要刷新 lockfile。
- 修改公开 API、协议字段、端点、端口、生成目录或运行流程时，同步检查调用方、样板和文档。

## 验证与交付

- 根据目标目录 `AGENTS.md` 的要求执行最小充分验证。
- 新增协议解析、命名转换、重连状态、生成器诊断或文件替换等可独立验证的逻辑时，应优先补充自动化测试。
- 无法验证时，交付说明必须列出未执行的命令和原因，不能声称已经通过。
- `README.md` 面向首次使用者；`FRAMEWORK_DESIGN.md` 描述架构边界；`FRAMEWORK_RUNTIME.md` 描述当前实际运行机制。不要把规划能力写成已经实现。
- 涉及对外功能、启动命令或许可证说明时，同步检查 `README_EN.md`。
