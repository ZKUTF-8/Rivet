---
description: rivet.stress.test — 压测应用编码规则
globs: src/web/apps/rivet.stress.test/**/*.{vue,ts}
alwaysApply: false
---

# rivet.stress.test 压测应用编码规则

## 项目定位

压测应用是一个 Vue 3 + Vite 8 单页应用，用于验证 Rivet 框架的 SignalR 通信性能：

- 可配置变量数量（1 ~ 10000）、更新频率（5ms ~ 2000ms）
- 支持批量推送 vs 逐个推送对比
- 支持 MessagePack vs JSON 协议实时切换
- 实时展示延迟（平均/P95/P99）、FPS、吞吐量等指标

## 编码要求

- 使用 `<script setup lang="ts">` 语法
- 组件 props 使用 `defineProps<T>()` 泛型定义
- 组件事件使用 `defineEmits<T>()` 泛型定义
- 样式优先沿用现有浅色工程工具风格；不要强行改成暗色主题
- 通过前端核心通信层（当前可为 `@rivet/client` workspace 引用，后续也可迁移为 `src/web/core` 本地引用）与后端通信，避免业务组件直接散落 SignalR 细节
- 组件内部状态、内部函数、非导出常量也需要中文 JSDoc/TSDoc 或必要注释；注释说明职责和设计原因，不要翻译代码
