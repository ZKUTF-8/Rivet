---
description: Rivet 项目全局规则 — 语言、编码风格、项目概览
alwaysApply: true
---

# Rivet 项目全局规则

## 语言要求

- **所有对话、回答、注释、文档一律使用中文（简体）**
- 仅在以下情况使用英文：代码标识符、类名、方法名、变量名、技术关键字（如 SignalR、MessagePack、Vue、Tauri）、包名、文件名
- git commit 消息使用中文

## 项目概览

Rivet 是一个 **.NET + Vue 上位机快速开发框架**，核心理念：

- 后端用 .NET 处理硬件/业务逻辑，前端用 Vue 做 UI 渲染
- 通过 `[JsCallable]`、`[JsBindable]`、`[JsEvent]` 三个特性标记，由 Source Generator 自动生成 SignalR Hub 方法和 TypeScript 代理
- 开发者只需要写后端逻辑 + 前端 UI，中间的通信层完全自动化
- Tauri 作为桌面壳子，通过 sidecar 拉起 .NET 进程

## 项目结构

```
src/server/     → .NET 本地服务层（核心库 / Source Generator / 业务或压测服务）
src/web/        → 前端 pnpm workspace（前端核心包 / 业务或压测应用 / shell CLI 包）
src/shell/      → 桌面壳层（Tauri 壳模板或可复用壳工程）
```

当前阶段采用单仓库组织：核心封装库和实际业务/压测项目放在同一个仓库内，通过本地项目引用或 workspace 引用协作。等 API 和工程形态稳定后，再将核心库剥离为 NuGet 包、npm 包和 Tauri 模板。

前端业务代码访问后端的全局对象统一命名为 `rv`。

目录命名统一使用小写加点号，例如 `rivet.core`、`rivet.stress.test.server`、`mysoow.toolkit`。各语言内部包名遵守本语言生态：C# 项目和命名空间使用 PascalCase，Rust crate 使用 snake_case，npm 包名按 npm 规范命名。

## 编码规范

- **所有公开的类、方法、属性、字段必须使用 XML 文档注释（C#）或 JSDoc/TSDoc 注释（TypeScript），用中文描述其用途**
- 私有成员如果逻辑不明显也应添加注释
- 不要写多余的"翻译代码"注释（如 `// 返回结果`），只解释"为什么"而非"是什么"
- 保持代码整洁，遵循各语言的命名规范

## 技术栈

- .NET 10 (LTS)，核心库目标 netstandard2.0
- Vue 3.5 + Vite 8 + TypeScript
- Tauri v2
- SignalR + MessagePack 二进制协议
- pnpm workspace 管理前端包
