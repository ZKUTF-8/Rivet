---
description: Rivet.DevApp — 开发/压测后端应用编码规则
globs: src/backend/Rivet.DevApp/**/*.cs
alwaysApply: false
---

# Rivet.DevApp 编码规则

## 项目定位

Rivet.DevApp 是开发和压力测试用的 ASP.NET Core 应用，目标框架 `net10.0`。当前职责：

- 提供 SignalR Hub（`BridgeHub`）作为前后端通信桥梁
- 通过 `StressTestService` 模拟真实上位机场景的变量同步
- 使用 MessagePack 二进制协议替代 JSON，提升高频推送性能
- 后端监听 `http://localhost:5000`，Hub 端点 `/bridge`

## 编码要求

- Hub 方法名使用 PascalCase（如 `StartStressTest`）
- 所有 Hub 方法和 Service 公开方法必须使用 XML 文档注释，中文说明
- 异步方法返回 `Task` 或 `Task<T>`，使用 `async/await`
- 线程安全：`StressTestService` 的共享状态使用 `lock` 保护
- 此项目中的 Hub 方法是手写的，后续会被 Source Generator 自动生成的代码替代
